# PDF Fixtures

Sample PDF files for viewer, search, annotation, and thumbnail tests.

Keep PDFs tiny and synthetic. Put corrupt or extension-only samples under
`invalid/` so preview tests can opt into failure cases deliberately.
