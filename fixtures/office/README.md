# Office Fixtures

Sample Office files for recognition, Quick Look preview, metadata, and extracted text tests.

Valid Office documents are intentionally not checked in yet to avoid large or
opaque binary fixtures. Add only synthetic documents with clear provenance.

The `invalid/` directory contains extension-only files for classification and
handoff error tests; do not use those as valid preview fixtures.
