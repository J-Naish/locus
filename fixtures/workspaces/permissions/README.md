# Permission Workspace

Use this directory for permission and metadata boundary tests. Git does not
portably preserve all read-only and ACL states, so tests may need to adjust
permissions in a temporary copy before asserting behavior.
