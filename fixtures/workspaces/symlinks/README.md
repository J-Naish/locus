# Symlink Workspace

This workspace includes symlinks created outside the patch format:

- `linked-folder` -> `target-folder`
- `linked-file.txt` -> `target-file.txt`

Listing code should report links as symlinks and should not traverse them.
