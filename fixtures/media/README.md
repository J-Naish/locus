# Media Fixtures

Small synthetic media samples for file type detection and preview handoff tests.

`valid/` contains text-based media that is safe to diff. `invalid/` contains
extension-only or corrupt files that should be used only by failure-path tests.
