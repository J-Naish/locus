# Config Fixtures

JSON, YAML, and TOML samples for structured text editing, validation, search,
and preview tests.

- `valid/`: well-formed files that should parse.
- `invalid/`: malformed files for diagnostics and recovery behavior.
- `edge/`: common real-world syntax edges that may or may not be supported by a
  specific parser.
