# Windows App

The Windows app is planned after the macOS MVP.

Expected stack:

- C#
- WinUI 3
- Windows App SDK
- WebView2 only where native controls are insufficient

The Windows app should share the Rust core with the macOS app.
